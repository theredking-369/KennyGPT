# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: kennygpt-api
metadata:
  template: kennygpt-api@0.0.1-beta

services:
  api:
    project: ./KennyGPT
    host: appservice
    language: dotnet
